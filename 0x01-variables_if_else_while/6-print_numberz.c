#include <stdio.h>

/**
 * main- The entry point
 *
 * Return: 0 Always (Success)
 *
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		putchar(num);
	}
	putchar('\n');
	return (0);
}
